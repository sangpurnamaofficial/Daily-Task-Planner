services:
  - type: web
    name: dailypulse-planner
    env: node
    plan: free
    buildCommand: ""
    startCommand: node server.js
    disk:
      name: planner-data
      mountPath: /app/data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
